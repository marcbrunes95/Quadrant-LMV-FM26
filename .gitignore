# dependencies
node_modules/

# env / secrets (never commit keys)
.env
.env.*
!.env.example

# Claude Code local settings (personal overrides)
.claude/settings.local.json
**/.claude/settings.local.json

# scratch / extraction artifacts
_x/
parse.js
seed.js

# private roster data (DNI/NIE) — never commit
quadrant-fm/supabase/_private/

# OS
.DS_Store
Thumbs.db
